SPARK:
======

val ordersRDD = sc.textFile("/public/retail_db/orders")

val ordersPairedRDD = ordersRDD.map( o=> (o.split(",")(0).toInt,o.split(",")(1).substring(0,10).replace("-","").toInt))

ordersPairedRDD.take(10).foreach(println)

val orderItemsRDD = sc.textFile("/public/retail_db/order_items")

val orderItemsPairedRdd = orderItemsRDD.map(oi => (oi.split(",")(0).toInt,oi))

orderItemsPairedRdd.take(10).foreach(println)
